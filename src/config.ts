/* ═══════════════════════════════════════════════════════════════════════
   YOUR SUPABASE KEYS — the only file that holds them
   ───────────────────────────────────────────────────────────────────────
   Paste them in once and never touch this file again. No future update
   will ever include it, so your keys cannot be wiped by an upload.

   Both come from Supabase → Project Settings:
     SUPABASE_URL  → Data API → the plain address, ending in .supabase.co
                     with nothing after it
     SUPABASE_KEY  → API Keys → the "default" publishable key,
                     starting sb_publishable_

   Never put the secret / service_role key in here. That one bypasses
   every security rule and must stay inside Supabase.
   ═══════════════════════════════════════════════════════════════════════ */

export const SUPABASE_URL = '';
export const SUPABASE_KEY = '';
